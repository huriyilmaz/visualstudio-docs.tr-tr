---
title: C6239
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6239
helpviewer_keywords:
- C6239
ms.assetid: c80e02bc-ff54-4fde-8c1c-5852853bed24
author: mikeblome
ms.author: mblome
manager: markl
ms.workload:
- multiple
ms.openlocfilehash: 5270a1c1092e14216473db5352785a64eee8ceed
ms.sourcegitcommit: 535ef05b1e553f0fc66082cd2e0998817eb2a56a
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 10/07/2019
ms.locfileid: "72013528"
---
# <a name="c6239"></a>C6239
Uyarı C6239: (\<sıfır olmayan sabit > & & \<expression >) her zaman \<expression > sonucunu değerlendirir. Bit düzeyinde and işlecini kullanmak mı istiyordunuz?

 Bu uyarı, bir test bağlamında oluşan mantıksal ve işlemin sol tarafında sıfır olmayan bir sabit değerin, diğeri olduğunu gösterir. Örneğin, `( 2 && n )` ifadesi, `n` ' nin Boolean değeri olan `(!!n)` ' e düşürülür.

 Bu uyarı genellikle bit düzeyinde AND (`&`) işlecinin kullanılması gereken bir bit maskesini denetleme denemesinin olduğunu ve sıfır olmayan sabit kod yollarının seçmeli olarak seçilmesinde 1 olarak değerlendiriliyorsa üretilmediğini belirtir.

## <a name="example"></a>Örnek
 Aşağıdaki kod bu uyarıyı oluşturur:

```
#include <stdio.h>
#define INPUT_TYPE 2
void f( int n )
{
   if(INPUT_TYPE && n) // warning 6239
   {
      puts("boolean value of n is true");
   }
   else
   {
      puts("boolean value of n is false");
   }
}
```

 Bu uyarıyı düzeltmek için aşağıdaki kodda gösterildiği gibi bit düzeyinde AND (`&`) işlecini kullanın:

```
#include <stdio.h>
#define INPUT_TYPE 2
void f( int n )
{
   if( ( INPUT_TYPE & n ) )
   {
      puts("bitmask true");
   }
   else
   {
      puts("bitmask false");
   }
}
```

## <a name="see-also"></a>Ayrıca Bkz.
 [& İşleci](/dotnet/csharp/language-reference/operators/and-operator)