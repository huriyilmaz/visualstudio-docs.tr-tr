---
title: 'CA5398: sabit kodlanmış SslProtocols değerlerinden kaçının'
description: Kod Analizi kuralı CA5398, nedenler, ihlallerin nasıl düzeltileceğini ve ne zaman bastıralınacağını içeren bilgiler sağlar.
ms.date: 08/14/2019
ms.topic: reference
author: dotpaul
ms.author: paulming
manager: jillfra
dev_langs:
- CSharp
- VB
ms.workload:
- multiple
f1_keywords:
- CA5398
ms.openlocfilehash: f3f7429ff2bfd31a30fb14fdfae1d193f44aea92
ms.sourcegitcommit: 5421999c9ff05b3242d33e4b0465f8bc29c7c643
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 01/07/2020
ms.locfileid: "75718629"
---
# <a name="ca5398-avoid-hardcoded-sslprotocols-values"></a>CA5398: sabit kodlanmış SslProtocols değerlerinden kaçının

|||
|-|-|
|TypeName|SslProtocolsAnalyzer|
|CheckId|CA5398|
|Kategori|Microsoft.Security|
|Son değişiklik|Kırılmamış|

## <a name="cause"></a>Sebep

Bu kural, aşağıdaki koşullardan biri karşılandığında ateşlenir:
- Güvenli ancak sabit kodlanmış <xref:System.Security.Authentication.SslProtocols?displayProperty=nameWithType> değere başvuruldu.
- Güvenli protokol sürümünü temsil eden bir tamsayı değeri, bir <xref:System.Security.Authentication.SslProtocols> değişkenine atandı, <xref:System.Security.Authentication.SslProtocols> dönüş değeri olarak kullanılır veya <xref:System.Security.Authentication.SslProtocols> bağımsız değişkeni olarak kullanılır.

Güvenli değerler şunlardır:
- Tls12
- Tls13

## <a name="rule-description"></a>Kural açıklaması

Aktarım Katmanı Güvenliği (TLS), genellikle Köprü Metni Aktarım Protokolü güvenli (HTTPS) ile bilgisayarlar arasındaki iletişimin güvenliğini sağlar. Protokol sürümleri TLS 1,0 ve TLS 1,1 kullanım dışıdır, ancak TLS 1,2 ve TLS 1,3 geçerli bir özelliktir. Daha sonra, TLS 1,2 ve TLS 1,3 kullanım dışı bırakılmış olabilir. Uygulamanızın güvenli kalmasını sağlamak için bir protokol sürümünü kodlamadan kaçının. Daha fazla bilgi için, [.NET Framework Ile Aktarım Katmanı Güvenliği (TLS) en iyi uygulamaları](/dotnet/framework/network-programming/tls)bölümüne bakın.

## <a name="how-to-fix-violations"></a>İhlalleri çözme

TLS protokolü sürümlerini kullanmayın.

## <a name="when-to-suppress-warnings"></a>Uyarıların ne zaman bastırılamıyor

Gelecekteki TLS protokol sürümlerini kullanmak için yükseltilemeyen eski bir hizmete bağlanmanız gerekir.

## <a name="pseudo-code-examples"></a>Sözde kod örnekleri

### <a name="enumeration-name-violation"></a>Sabit listesi adı ihlali

```csharp
using System;
using System.Security.Authentication;

public class ExampleClass
{
    public void ExampleMethod()
    {
        // CA5398 violation
        SslProtocols sslProtocols = SslProtocols.Tls12;
    }
}
```

```vb
Imports System
Imports System.Security.Authentication

Public Class TestClass
    Public Function ExampleMethod() As SslProtocols
        ' CA5398 violation
        Return SslProtocols.Tls12
    End Function
End Class
```

### <a name="integer-value-violation"></a>Tamsayı değer ihlali

```csharp
using System;
using System.Security.Authentication;

public class ExampleClass
{
    public SslProtocols ExampleMethod()
    {
        // CA5398 violation
        return (SslProtocols) 3072;    // TLS 1.2
    }
}
```

```vb
Imports System
Imports System.Security.Authentication

Public Class TestClass
    Public Function ExampleMethod() As SslProtocols
        ' CA5398 violation
        Return CType(3072, SslProtocols)   ' TLS 1.2
    End Function
End Class
```

### <a name="solution"></a>Çözüm

```csharp
using System;
using System.Security.Authentication;

public class TestClass
{
    public void Method()
    {
        // With .NET Framework 4.7 or later, let the operating system decide what TLS protocol version to use.
        // See https://docs.microsoft.com/dotnet/framework/network-programming/tls
        SslProtocols sslProtocols = SslProtocols.None;
    }
}
```

```vb
Imports System
Imports System.Security.Authentication

Public Class TestClass
    Public Sub ExampleMethod()
        ' With .NET Framework 4.7 or later, let the operating system decide what TLS protocol version to use.
        ' See https://docs.microsoft.com/dotnet/framework/network-programming/tls
        Dim sslProtocols As SslProtocols = SslProtocols.None
    End Sub
End Class
```

## <a name="related-rules"></a>İlgili kurallar

[CA5364: kullanım dışı güvenlik protokollerini kullanma](ca5364.md)

[CA5386: SecurityProtocolType değerinden hardmaktan kaçının](ca5386.md)

[CA5397: kullanım dışı SslProtocols değerlerini kullanma](ca5397.md)
